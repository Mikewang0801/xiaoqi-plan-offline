import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.jihuabiao.mobile.offline",
  appName: "小淇计划·离线版",
  webDir: "dist",
  backgroundColor: "#FFF7F1",
  android: {
    backgroundColor: "#FFF7F1",
  },
};

export default config;
